package src.core.entities.employees;

public class Admin extends Employee {
    public Admin(String nome, int ID, String endereco, String contato, String dataRegistro, String login, String password) {
        super(nome, ID, endereco, contato, dataRegistro, login, password);
    }
    
}
