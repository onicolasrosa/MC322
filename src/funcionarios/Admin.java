package src.funcionarios;

public class Admin extends Funcionario {
    protected Admin(String nome, String ID, String endereco, String contato, String dataRegistro) {
        super(nome, ID, endereco, contato, dataRegistro);
    }
    
}
